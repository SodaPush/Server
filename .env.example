# Required: a random 32-byte base64url key. Back it up; losing it makes encrypted data unreadable.
# The interactive Cloudflare deployment script generates this automatically.
MASTER_KEY=

# Required: a long, private token (at least 20 characters) for the first owner bootstrap.
# The interactive Cloudflare deployment script asks for this value.
BOOTSTRAP_TOKEN=
